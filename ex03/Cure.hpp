#ifndef CURE_H
#define CURE_H

class Cure {
	public:
		// Default constructor
		Cure();

		// Copy constructor
		Cure(const Cure &other);

		// Copy assignment operator
		Cure& operator=(const Cure &other);

		// Destructor
		~Cure();

	private:
		// Class members
};

#endif // CURE_H
